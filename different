Git和SVN都是用来对工程进行版本控制，二者的不同之处在于它们的架构原理。

Git是分布式的，而SVN是集中式的。Git允许本地仓库脱离远程服务器仓库进行本地工程的版本控制，而使用SVN每次提交时，都需要和中心服务器仓库进行同步。

分支概念不同。分支在SVN上是一个完整的目录（复制一份完整的工程），占用空间大。如果创新新分支，会同步到所有人，影响大。Git下的分支在合并之前不会影响到人和人。

Git是把工程的内容按照元数据的方式存储，而SVN是把工程的内容按照文件的方式存储。
